import styled from '@emotion/styled';

export const Logo = styled.div`
  display: flex;
  align-items: center;
  cursor: pointer;

  .logo__image {
    width: 4rem;
    margin-right: 1rem;
  }

  .logo__text {
    font-size: 2rem;
    font-weight: bold;
  }
`;
