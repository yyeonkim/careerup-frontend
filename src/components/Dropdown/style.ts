import styled from '@emotion/styled';

export const List = styled.ul`
  position: absolute;
  bottom: -10rem;
  background-color: white;
  border-radius: 1rem;
  font-size: 1.4rem;
  padding: 1rem 2rem;

  li {
    padding: 1rem 0;
    cursor: pointer;
  }
`;
